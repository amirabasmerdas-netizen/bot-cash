services:
  - type: web
    name: bot-price-analyzer-webhook
    env: python
    region: frankfurt
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: WEBHOOK_URL
        sync: false
      - key: PORT
        value: 8443
    healthCheckPath: /health
    autoDeploy: true
    plan: free
