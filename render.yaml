services:
  - type: web
    name: bot-professional-v16
    env: python
    region: frankfurt
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: PORT
        value: 8443
    healthCheckPath: /health
    autoDeploy: true
    plan: free
